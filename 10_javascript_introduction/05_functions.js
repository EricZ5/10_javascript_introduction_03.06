function sum(x, y) {
    let ergebnis = x + y;
    return ergebnis;
}

sum(10, 2);
console.log("Ergebnis = " + sum(2, 3));

function sub(x,y)
{
    let ergebnis2 = x+y;
    return ergebnis;
}
//sub(8, 4);
console.log("Ergebnis= " + sub(8,4));
/*
AUFGABE:
1) Funktion anlegen für Subtraktion oder Multiplikation oder Division
2) Aufruf dieser Funktion
*/